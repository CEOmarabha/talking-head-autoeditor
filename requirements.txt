# Deliberately small. Everything else is FFmpeg. Release builds use the
# platform lock files under packaging/, including hashes for every dependency.
faster-whisper==1.2.1     # word-level transcription (CTranslate2, CPU-friendly)
auto-editor==29.3.1       # loudness-based cutting (fallback path only)
pillow==12.3.0            # caption and card rendering (no libass required)
numpy==2.5.1              # sync verification cross-correlation
