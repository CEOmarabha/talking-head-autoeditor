{
  "name": "autoeditor-web-worker",
  "private": true,
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/integration.mjs",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "miniflare": "5.20260801.1-alpha",
    "wrangler": "4.120.0"
  }
}
